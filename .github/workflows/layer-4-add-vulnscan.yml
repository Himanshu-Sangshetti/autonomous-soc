# Progressive talk demo (4/6): Adds Grype scanning the Syft SBOM JSON.
name: "Layer 4: + Vulnerability Scanning"
on:
  workflow_dispatch:
    inputs:
      scenario:
        description: 'Scenario'
        required: false
        default: 'clean'
        type: choice
        options: [clean, malicious-dep, leaked-secret, vuln-dep]

permissions:
  contents: read

jobs:
  layer4:
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@5ef0c079ce82195b2a36a210272d6b661572d83e
        with:
          egress-policy: audit
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Apply scenario
        if: github.event.inputs.scenario != 'clean'
        run: |
          SCENARIO="${{ github.event.inputs.scenario }}"
          cp "demo/scenarios/${SCENARIO}/package.json" demo/app/package.json 2>/dev/null || true
          cp "demo/scenarios/${SCENARIO}/mcp-config.json" demo/app/mcp-config.json 2>/dev/null || true

      - name: Install (safe)
        working-directory: demo/app
        run: npm install --ignore-scripts

      - name: Generate SBOM
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin 2>/dev/null
          syft packages dir:demo/app -o cyclonedx-json --file=/tmp/soc-sbom-current.json

      - name: SBOM Diff
        run: |
          echo "[]" > /tmp/soc-signals.json
          python3 tools/sbom-diff.py /tmp/soc-sbom-current.json --signals-file /tmp/soc-signals.json || true

      - name: Provenance Check
        run: |
          cd demo/app && npm audit signatures > /tmp/provenance-output.txt 2>&1 || true
          cat /tmp/provenance-output.txt

      - name: Vulnerability Scan
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin 2>/dev/null
          grype sbom:/tmp/soc-sbom-current.json -o json --file=/tmp/grype-output.json || true
          echo "=== HIGH/CRITICAL CVEs ==="
          python3 -c "
import json
try:
    d = json.load(open('/tmp/grype-output.json'))
    hits = [m for m in d.get('matches',[]) if m.get('vulnerability',{}).get('severity','').lower() in ('high','critical')]
    print(f'Found {len(hits)} high/critical vulnerabilities')
    for m in hits[:10]:
        v = m.get('vulnerability',{})
        a = m.get('artifact',{})
        print(f'  [{v.get(\"severity\")}] {v.get(\"id\")} in {a.get(\"name\")}@{a.get(\"version\")}')
except: print('No grype output')
"
